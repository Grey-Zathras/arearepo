<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('../templates/partials/head', {title: 'Create New Game Room'}); %>
</head>
<body>
  <%- include('../templates/partials/header', {headline: 'Create a New Game Room'}); %>
  <div class="body-main">
    <form action="/create" method="POST">
      <div>
        <label for="title">Title:</label>
        <input type="text" id="title" name="title" required><br><br>
      </div>
      <div style="display: none;">
        <label for="code">Code:</label>
        <input id="code" name="code" required value="<%= code %>" maxlength="8" size="8"><br><br>
      </div>
      <button type="submit">Create Room</button><br><br>
    </form>
    <a href="/">Back to home</a>
  </div>
</body>
</html>
